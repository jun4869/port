<h1><%= @community.title %></h1>

<% @posts.each do |post| %>
  <div>
    <p><%= post.contributor %></p>
    <p><%= simple_format(post.content) %></p>
    <p><%= post.created_at %></p>
  </div>
<% end %>
